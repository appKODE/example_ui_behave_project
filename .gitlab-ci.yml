before_script:
  - python --version
  - mkdir -p /qa-builds
  - ls /qa-builds

stages:
  - test

test:
  stage: test
  only:
   - master
  tags:
   - qa-serv
  script:
   - pip install -r requirements.txt
   - pytest -v main.py

