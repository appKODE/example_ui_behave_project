image: python:3.6

before_script:
  - python --version
  - appium-doctor

stages:
  - test

test:
  stage: test
  only:
   - master
  tags:
   - qa-serv
  script:
   - pip install -r requirements.txt
   - python3 main.py

