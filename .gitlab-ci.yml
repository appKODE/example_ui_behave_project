image: appium-container

before_script:
  - python --version

stages:
  - test

test:
  stage: test
  only:
   - master
  tags:
   - qa-appium
  script:
   - pip install -r requirements.txt
   - pytest -v main.py

