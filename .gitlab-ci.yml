image: butomo1989/docker-android-x86-7.1.1

before_script:
  - python3 --version

stages:
  - test

test:
  stage: test
  only:
   - master
  tags:
   - qa-appium
  script:
   - pip install -r requirements.txt
   - pytest -v main.py

